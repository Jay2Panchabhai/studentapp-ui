pipeline {
    agent {
        label 'worker'
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/mayurpandit25/studentapp-ui.git'
                echo 'Cloning is successful'
            }
        }
    }
}